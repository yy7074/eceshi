# 当前代码状态

更新时间：2026-04-05

## 仓库状态

- 项目结构仍然是 `frontend/` 的 uni-app 前端和 `backend/` 的 FastAPI 后端。
- 根目录文档已做一轮收口，历史阶段性报告已迁入 `docs/archive/`。
- `frontend/` 和 `backend/` 目录下的阶段性说明文档也已归档，目前各自只保留一个 `README.md` 作为入口。
- 当前工作区存在未提交代码变更，主要在后台管理相关文件：
  - `backend/admin/index.html`
  - `backend/app/api/v1/admin.py`
  - `backend/app/models/laboratory.py`
  - `backend/app/models/role.py`

## 需要优先注意的问题

### 1. 后台页面冲突标记已清理，但后台改动仍未完成收口

- `backend/admin/index.html` 中遗留的冲突标记已清理。
- 订单详情相关状态也已拆分为“只读详情”和“编辑表单”两套，避免两个弹窗共用同一状态。
- 但后台管理相关文件整体仍处于未提交状态，后续联调仍应优先关注后台订单管理链路。

### 2. 文档与代码并不完全一致

历史文档里有几类问题需要按代码重新核对，不能直接相信旧结论：

- 预约下单页价格曾被记录为硬编码风险，相关说明已归档到 `docs/archive/root/`。
- 钱包页曾被记录为余额/账单接口未完全对接，相关说明已归档到 `docs/archive/root/`。
- 用户余额接口曾出现字段为 `null` 的测试记录，相关说明已归档到 `docs/archive/root/`。
- 认证流程曾有前后端字段模型不一致的问题，相关说明已归档到 `docs/archive/root/`。

## 当前建议

1. 先按实际代码重新验证后台订单管理和指派流程。
2. 再复查预约下单、钱包、认证这几条主链路。
3. 后续新增文档尽量只保留“入口文档 + 当前状态 + 必要部署说明”，阶段性汇报直接放到 `docs/archive/`。
